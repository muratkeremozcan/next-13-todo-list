import New from './page'
// import * as todoServer from '../todo.server'

// TODO: make a component with 'use server' work
// error: Cannot find module 'react-server-dom-webpack/client'
describe.skip('<New />', () => {
  it('renders', () => {
    // cy.stub(todoServer, 'createTodo').returns(Promise.resolve()) // wish this worked
    // cy.mount(<New />)
  })
})
